const playGuessingGame = (numToGuess, totalGuesses) => {
  let userInput = prompt("Enter a number from 1 to 100");
  if (userInput != null) {
  }
};

const numToGuess = Math.random().toFixed();
const totalGuesses = 4;
playGuessingGame(numToGuess, totalGuesses);
